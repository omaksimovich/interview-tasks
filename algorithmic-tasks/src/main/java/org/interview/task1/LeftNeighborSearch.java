package org.interview.task1;

public class LeftNeighborSearch {

    /*
       Problem Requirements:
   Find the value: If the value exists in the array, return it.
   If not found, return the largest value that is smaller than the target (the left neighbor).
   If no left neighbor exists (i.e., the target is smaller than the first element), return -1.
   */
    public static int findValueOrLeftNeighbor(int[] arr, int target) {

        return -1;

    }
}
